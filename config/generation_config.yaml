generation:
  attribute_target: [2, 2, 2]  # [enemies, gaps, pipes]
  num_levels: 1
  columns_per_level: 100
  temperature: 0.8
  top_k: 20
  top_p: 1.2
  cfg_scale: 1.3
models:
  mamba_path: checkpoints/mamba_best_ema.pth
output:
  directory: output/generated_levels
  format: txt
  visualize: true
