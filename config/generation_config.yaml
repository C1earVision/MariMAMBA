generation:
  attribute_target: [3, 0, 3]  # [enemies, gaps, pipes]
  num_levels: 1
  columns_per_level: 120
  temperature: 0.8
  top_k: 30
  top_p: 1.5
  cfg_scale: 1.2
models:
  mamba_path: checkpoints/mamba_best.pth
output:
  directory: output/generated_levels
  format: txt
  visualize: true
