# ── Runtime: packages needed to run the Gradio demo ─────────────────────────
# (Also what Hugging Face Spaces installs)

# Core ML
torch>=2.0.0
numpy>=1.24.0

# Image rendering
pillow>=9.5.0

# Level rendering (MarioGPT sprites)
# transformers must be pinned below 4.40 — AutoModelWithLMHead was removed in 4.40
transformers<4.40.0
mario_gpt

# HTTP calls to Groq LLM API
requests>=2.28.0

# Gradio UI
gradio==6.12.0