# ── Runtime: packages needed to run the Gradio demo ─────────────────────────

# Core ML
torch>=2.0.0
numpy>=1.24.0

# Image rendering
pillow>=9.5.0

# Level rendering (MarioGPT sprites)
# transformers must be pinned below 4.40 — AutoModelWithLMHead was removed in 4.40
transformers<4.40.0
mario_gpt

# HTTP calls to Groq LLM API
requests>=2.28.0

# Gradio UI
gradio==6.12.0

# Training loop progress bars
tqdm>=4.65.0

# Loss curve plotting (used by mamba_trainer.py and evaluate_model.py)
matplotlib>=3.7.0

# Config parsing in evaluate_model.py
pyyaml>=6.0

# Hyperparameter optimization (scripts/optimize_mamba.py)
optuna>=3.0.0